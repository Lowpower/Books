#include <iostream>
using namespace std;

const int MAX = 500;
struct Node {
    int p, l, r;
};

struct Node T[MAX];

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        T[i] = nil;
    }
    return 0;
}