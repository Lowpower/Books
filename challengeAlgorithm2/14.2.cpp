#include <cstdio>
#include <vector>
#include <algorithm>

using namespace std;

class Node {
    int location;
    int p, l, r;
    Node(){}
};
int N;
static const int MAX = 1000000;
static const int NIL = -1;
Node P[MAX];

int main() {
    int x, y;
    scanf("%d", N);
    for (int i = 0; i < N; i++) {
        scanf("%d %d", &x, &y);

    }
    return 0;
}